mosquitto_pub -h test.mosquitto.org -t /UdeA/PI1/data -m "Hola"

db.js
/* var con = mysql.createConnection({
    host: "localhost",
    database: "DB_PI",
    user: "root",
    password: "password",
    port: "3307"
  }); */
  
/*con.connect(function(err) {
  if (err) throw err;
  con.query('SELECT * FROM Student', (err,rows) => {
    if(err) throw err;
  
    console.log('Data received from Db:');
    console.log(rows);
  });
});*/